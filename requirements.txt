# ===== 多AGV调度仿真 依赖清单（Windows 10 + Python 3.12，全部免费开源）=====
# 版本为本仓库实际验证过的组合，直接 pip install -r requirements.txt 即可复现。
numpy==2.5.1        # 数值计算：曼哈顿距离、泊松任务流(rng)、压测统计指标
flask==3.1.3        # Web 看板后端：REST 状态接口 + 页面服务
pymodbus==3.6.9     # Modbus/TCP 协议栈：模拟 4 个站台呼叫盒从站 + 客户端自测
